{
  "name": "server",
  "version": "1.0.0",
  "description": "Loqation Dashboard Backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "cd ../client && npm run build && cd ../server && rm -rf public && mkdir public && cp -r ../client/dist/* public/ && gcloud app deploy --version=live --no-promote"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "google-auth-library": "^10.5.0",
    "googleapis": "^140.0.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.10.0"
  }
}
